{
    "imports": {
        "/": "./src/",
        "./": "./",
        "deno_testing": "https://deno.land/std@0.181.0/testing/asserts.ts"
    },
    "tasks": {
        "coverage": "rm -r dev/coverage/ && deno test --coverage=dev/coverage/ && deno coverage dev/coverage/ --lcov --output=dev/coverage/lcov.info"
    }
}